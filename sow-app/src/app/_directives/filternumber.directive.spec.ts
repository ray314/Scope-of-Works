import { FilternumberDirective } from './filternumber.directive';

describe('FilternumberDirective', () => {
  it('should create an instance', () => {
    const directive = new FilternumberDirective(null);
    expect(directive).toBeTruthy();
  });
});
